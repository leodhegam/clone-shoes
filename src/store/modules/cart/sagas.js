// import { call, put, all, takeLatest } from 'redux-saga/effects';
// import products from '../../../server.json';

// function* addCart({ id }) {
//   const response = yield call(products.get, `${id}`);
//   console.log(response.data);
//   yield put(addCartSuccess(response.data));
// }

// export default all([takeLatest('ADD_CART_REQUEST', addCart)]);
